#include <iostream>
#include <chrono>
#include <vector>
#include <fstream>

using std::cout;
using std::cin;

using std::endl;
using std::vector;
using std::swap;
using std::toupper;

int main() {
    char lista[] {" São Pualo "};
    cout << char(toupper(lista[10])) << endl;
    return 0;
}